/**
 * Placeholder for Supabase database types.
 *
 * Replace this file with the output of `supabase gen types typescript` once
 * the schema is applied to the project:
 *
 *   npx supabase gen types typescript --project-id <project-id> > src/lib/supabase/types.ts
 *
 * Until then, `Database` is typed as `unknown` so the rest of the codebase
 * can import it without errors.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
